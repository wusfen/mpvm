<view>
model: {{model}}
</view>
<view>
obj.key: {{obj.key}}
</view>
<view />
computed.total: {{total}}
<view />
==
<view>
  <view wx:for="{{arr}}">
    {{item}}
    {{item.name}}
    <button id="{{item.id}}" bind:tap="click">button</button>
  </view>
</view>