{{model}}
<view />
{{obj.key}}
<view />
{{arr[0]}}
<view />
computed: {{computed}}
<view />
==

<button id="{{obj.key}}" bind:tap="click">it</button>
==
<view>

  <view wx:for="{{arr}}">
    {{item}}
    {{item.name}}
    <button id="{{item.id}}" bind:tap="click">button</button>
  </view>
</view>